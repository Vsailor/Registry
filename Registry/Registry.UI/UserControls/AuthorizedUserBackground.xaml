<UserControl x:Class="Registry.UI.UserControls.AuthorizedUserBackground"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Registry.UI.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Row="0">
            <Button Name="SignOutButton" HorizontalAlignment="Right" Click="SignOutButton_Click" Margin="0,0,10,0" Width="80">Вийти</Button>
        </Grid>
        <Grid Row="1" Name="MainGrid"></Grid>
    </Grid>
</UserControl>
